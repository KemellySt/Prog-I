#include <stdio.h>
#include <string.h>
int main()
{
    //Introduzindo o programa
    printf("\n------------------------Este programa transforma letras minúsculas em maiúculas-----------------------------\n");
    //Declarando variáveis
    char texto[50], substituido;
    int contador = 0;
    //Imprimindo a pergunta
    printf("Digite o texto: ");
    scanf(" %[^\n]s", texto);
    printf("Digite o caracter a ser substituido: ");
    scanf(" %[^\n]s", )
    //Condição
    for(int aux = 0; aux != '\0' ; aux++)
    {
        if(texto[aux] == substituido)
        {
            
        }
    }
    //Imprimindo para o usuário
    printf("String Original\n");
    printf("%s", texto);
    printf("String Nova\n");
    printf("%s, %s", texto, )




    return 0;
}